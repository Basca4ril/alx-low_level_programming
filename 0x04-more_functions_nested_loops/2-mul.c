#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies 2 integers
 *
 * @a: first parameter for mul
 * @b: second parameer for mul
 * Return: multiple of a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
